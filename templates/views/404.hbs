<!DOCTYPE html>

<html>
  <header>
    <title>404</title>
    <link rel="stylesheet" href="./css/styles.css" />
  </header>

  <body>
    <div class="main-content">
      {{>header}}
      {{>error}}
      {{message}}
    </div>
    {{>footer}}
  </body>
</html>

